import classes from './TopBar.modules.css';

function TopBar () {
    return(
        <header className={classes.header}>
        <div className={classes.socialmedia}>
        </div>
        <nav className={classes.nav}>
        </nav>

        </header>
        
    )

}

export default TopBar;